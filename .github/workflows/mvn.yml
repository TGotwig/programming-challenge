name: ⚡️ Java CI

on:
  push:
    branches: [challenge-weatherdata]

jobs:
  Ubuntu:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: 👷 Build with Maven
        run: mvn -B package --file pom.xml
      - name: 🧪 Test with Maven
        run: mvn test

  Windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: 👷 Build with Maven
        run: mvn -B package --file pom.xml
      - name: 🧪 Test with Maven
        run: mvn test
